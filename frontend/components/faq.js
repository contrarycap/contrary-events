import { Stack, Typography } from "@mui/material";

export default function FAQ() {
 return (
   <Stack spacing={2} width={'100%'}>
     <Typography variant="h2">
       FAQ
     </Typography>
     <Typography variant="body1">
       frequent questions about contrary
     </Typography>
   </Stack>
 )
}