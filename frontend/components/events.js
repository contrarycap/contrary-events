import { Stack, Typography } from "@mui/material";

export default function Events() {
 return (
   <Stack spacing={2} width={'100%'}>
     <Typography variant="h2">
       Events
     </Typography>
     <Typography variant="body1">
       show the events here
     </Typography>
   </Stack>
 )
}